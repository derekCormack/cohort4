// --------------------- The functions FUNCTION -----------------------

// var num1 = 0;
// var num2 = 0;
// var calcTotal = 0;

console.log("hello world")

const functions = {
    
    // size: (num) => {
    //     if (num < 10) return "small";
    //     if (num < 20) return "medium";
    //     return "large";
    // },

    add: (num1, num2) => {
        return  calcTotal =Number(num1) + Number(num2);        
        
    },

    subtract: (num1, num2) => {
        return calcTotal = Number(num1) - Number(num2); 
        
    },

    multiply: (num1, num2) => {
        return calcTotal = Number(num1) * Number(num2); 
    },

    divide: (num1, num2) => {
        return calcTotal = Number(num1) / Number(num2); 
    },

 };

export default functions;
